#pragma once
#include <vector>
#include "datestruct.h"
#include <vector>
#include <string>

std::vector<Datestruct> stringtodatestruct(std::vector<std::string> unformattedlessons, const char formatting);